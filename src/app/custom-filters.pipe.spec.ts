import { CustomFiltersPipe } from './custom-filters.pipe';

describe('CustomFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
